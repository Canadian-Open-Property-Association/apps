/**
 * Standard vertical divider for navigation bars
 * Used to separate sections in the app nav bar
 */
export default function NavDivider() {
  return <div className="w-px h-6 bg-gray-300 mx-2" />;
}
